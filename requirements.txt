# Django 5+ / 6+ require MariaDB 10.6+. This project targets MariaDB 10.5.x — use Django 4.2 LTS.
# After the server runs MariaDB 10.6+, you can switch to Django>=6.0,<7.
Django>=4.2,<5
Pillow>=10,<12
PyMySQL>=1.1,<2
mysqlclient>=2.2,<3 ; sys_platform != "darwin"
polib>=1.2,<2
